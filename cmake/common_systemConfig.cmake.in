@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/common_systemTargets.cmake")

# Provide old-style variables for compatibility
set(common_system_FOUND TRUE)
set(common_system_INCLUDE_DIRS "@CMAKE_INSTALL_PREFIX@/@CMAKE_INSTALL_INCLUDEDIR@")

# Check for required components (future extension point)
check_required_components(common_system)