# Repository events and Pages deployment for common_system.
# Doxygen generation remains in the reusable workflow.
name: Generate-Documentation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: documentation-pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  docs:
    permissions:
      contents: read
    uses: ./.github/workflows/doxygen.yml
    with:
      pages-artifact: true

  deploy:
    needs: docs
    if: >-
      github.repository == 'kcenon/common_system' &&
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    permissions:
      pages: write
      id-token: write
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
